[package]
name = "charon6"
version = "0.1.0"
edition = "2024"
description = "Covert data transmission via IPv6 destination addresses"
license = "MIT"

[dependencies]
clap = { version = "4.6.1", features = ["derive"] }
nix = { version = "0.31", features = ["socket", "net"] }
